apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: nodejs-express-apiserver
spec:
  selector:
    matchLabels:
      app.kubernetes.io/part-of: nodejs-express-apiserver
  endpoints:
  - port: 8080-tcp
    interval: 5s
    path: /metrics
    scheme: http